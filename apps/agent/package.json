{
  "name": "agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run main.py",
    "start": "uv run main.py",
    "lint": "uv run ruff check .",
    "format": "uv run ruff format ."
  }
}
