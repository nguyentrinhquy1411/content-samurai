{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run main.go",
    "build": "go build -o bin/api main.go",
    "start": "./bin/api",
    "lint": "golangci-lint run",
    "format": "gofmt -w ."
  }
}
